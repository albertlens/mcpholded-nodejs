# Archivo de ejemplo para Docker Compose
# Copia este archivo como .env y configura tu API key real

# API Key de Holded (OBLIGATORIO)
# Ve a: https://app.holded.com/settings/integrations/api
HOLDED_API_KEY=YOUR_HOLDED_API_KEY

# Puerto del servidor (ya configurado en docker-compose.yml)
PORT=3000

# Entorno
NODE_ENV=production
